a = input("a: ")
b = input("b: ")

temp=a                    #First we assign the value a to temp
a=b                       #Here value of b is assigned to a
b=temp                    #Value of temp which is (a) is assigned to b here

print("a: " + a)
print("b: " + b)
