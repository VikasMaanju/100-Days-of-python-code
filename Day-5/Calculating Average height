#########
student_heights = input("Input a list of student heights ").split()
for n in range(0, len(student_heights)):
  student_heights[n] = int(student_heights[n]
print(student_heights) 
#########
                          

#Here initially we took total_height =0 because we have to identify this variable 

total_height = 0
for height in student_heights:
  total_height += height
print(f"total height = {total_height}")

#In this for loop number of student is calculated using for loop
#We can do the same by using len() function but here we are working on for loop 

number_of_students = 0
for student in student_heights:
  number_of_students += 1
print(f"number of students = {number_of_students}")
  
average_height = round(total_height / number_of_students)
print(average_height)
